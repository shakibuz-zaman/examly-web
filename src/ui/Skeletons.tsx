// Shimmer placeholders shaped like the real content (§3.6 — no Spin on primary surfaces).
export function SkeletonCard() {
  return <div className="ex-skeleton ex-skel-card" aria-hidden />;
}

export function SkeletonRow() {
  return <div className="ex-skeleton ex-skel-row" aria-hidden />;
}
